var b=document.getElementById("a");
alert(a.style.width);
var sheet=document.stylesheets(0);
var rules=sheet.cssRules||sheet.rules;
var rule=rules[0];
alert(rule.style.margin);
function fun(x)
{
    
    if(x==0);
    if(x==1);
    if(x==2);
    if(x==3);
    if(x==4);
}